#!/usr/bin/env ruby
# Transform input to uppercase with occasional stderr chatter
# Usage: upcase

$stdout.sync = true
$stderr.sync = true

while line = $stdin.gets
  sleep rand * 0.03
  warn "[debug] processing: #{line.bytesize} bytes" if rand < 0.3
  puts line.upcase
end
